# Findings Ledger — known bugs & root causes

Append-only memory of confirmed root causes so they are **never re-investigated**. Before investigating, search here. Each entry: symptom → root cause → code ref → status. Keep terse. Owned by knowledge-keeper; any agent may propose an entry with evidence.

Format: `STATUS` ∈ OPEN | FIX-PLANNED | FIXED | WONTFIX.

---

### F-001 · <symptom> · OPEN
- **Symptom**: <observed behavior — what the user/system sees>.  _(placeholder example — delete when you add a real finding)_
- **Root cause**: <root cause: exactly why the code/data produces the symptom>.
- **Code**: <file:line>.
- **Fix direction**: <1-3 options, no code>.

---
_Add new confirmed findings above this line. Cite a `file:line` or ruling for every claim._
